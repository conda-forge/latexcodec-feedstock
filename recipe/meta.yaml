{% set name = "latexcodec" %}
{% set version = "1.0.6" %}
{% set sha256 = "e3ed029a17a168db4a1f0d87f81cff556879f229143cc5f5d49d7f894e29a74b" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/mcmtroffaes/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python
    - setuptools
    - six
  run:
    - python
    - six

test:
  imports:
    - latexcodec

about:
  home: https://github.com/mcmtroffaes/{{ name }}
  license: MIT
  license_family: MIT
  license_file: LICENSE.rst
  summary: 'Lexer and codec to work with LaTeX code in Python.'
  doc_url: http://latexcodec.readthedocs.io/
  dev_url:  https://github.com/mcmtroffaes/{{ name }}

extra:
  recipe-maintainers:
    - scopatz
